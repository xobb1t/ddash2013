---
- hosts: all
  sudo: true

  tasks:
  - name: update apt cache
    raw: apt-get update

  - name: install python-apt
    raw: apt-get -y install python-apt

  - name: fix locale settings
    raw: touch /etc/default/locale

  - name: upgrade system
    apt: upgrade=safe
